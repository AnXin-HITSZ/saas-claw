# pnpm 11 放行依赖的构建脚本（package.json 的 pnpm 字段 / onlyBuiltDependencies 均已不再读取，
# 设置迁移到 pnpm-workspace.yaml 且键名改为 allowBuilds: map 形式）。
# esbuild 的 postinstall 必须在 install 时执行（安装平台二进制），否则 vite 构建失败。
allowBuilds:
  esbuild: true