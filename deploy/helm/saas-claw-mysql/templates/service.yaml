apiVersion: v1
kind: Service
metadata:
  name: {{ include "saas-claw-mysql.fullname" . }}
  labels:
    {{- include "saas-claw-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: mysql
      port: {{ .Values.service.port }}
      targetPort: mysql
      protocol: TCP
  selector:
    {{- include "saas-claw-mysql.selectorLabels" . | nindent 4 }}
