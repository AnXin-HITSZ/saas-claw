apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "saas-claw-mysql.fullname" . }}-config
  labels:
    {{- include "saas-claw-mysql.labels" . | nindent 4 }}
data:
  saas-claw.cnf: |
    [mysqld]
    character-set-server={{ .Values.config.characterSetServer }}
    collation-server={{ .Values.config.collationServer }}
    {{- with .Values.config.extra }}
{{ . | indent 4 }}
    {{- end }}
