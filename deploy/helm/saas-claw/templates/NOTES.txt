Claw SaaS has been installed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Services deployed:
  gateway                    (port 8080)
  backend-for-frontend       (port 8081)
  claw-service               (port 8082)
  runtime-service            (port 8083)
  agent-marketplace-service  (port 8084)
  billing-service            (port 8085)
  skill-marketplace-service  (port 8086)
  pyclaw-runtime-api         (port 8090)

Check pod status:
  kubectl -n {{ .Release.Namespace }} get pods

Check gateway health:
  kubectl -n {{ .Release.Namespace }} port-forward svc/gateway 8080:8080
  curl http://localhost:8080/actuator/health
